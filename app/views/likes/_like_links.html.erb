<% if current_user.likes.find_by(prototype_id: prototype.id) %>
    <%= link_to 'いいね！を取り消す', unlike_path(prototype.id), method: :delete, remote: true %>
  <% else %>
    <%= link_to 'いいね！', like_path(prototype.id), method: :post, remote: true %>
<% end %>
