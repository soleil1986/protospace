<% if current_user.likes.find_by(prototype_id: prototype.id) %>
    <%= link_to image_tag("icon_heart.svg"), unlike_path(prototype.id), method: :delete, remote: true %>
  <% else %>
    <%= link_to image_tag("icon_heart_red.svg"), like_path(prototype.id), method: :post, remote: true %>
<% end %>
